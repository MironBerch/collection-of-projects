[project]
name = "dungeon"
version = "0.1.0"
description = "Dungeon game"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.2.6",
    "pygame>=2.6.1",
]

[dependency-groups]
dev = [
    "ruff>=0.11.12",
]
